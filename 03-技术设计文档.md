# APS 系统技术设计文档（V2.0）

# APS 系统技术设计文档（V2.0）

> 诚信边界：全部数值为演示环境仿真输出，不是工厂实测；平台未接入 ERP/MES，不产生生产指令；需求对应关系见《产品需求文档 V2.0》附录A。

---

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.x（历史） | 2026-09-18 | 全链路实施设计、用户端与对接技术方案分别成稿；一轮样式事故修复与端到端复测 |
| V2.0 | 2026-09-19 | 升版：业务可用性重构（同计划场景、版本化基础资料）、生产量级蒙特卡洛内核、逐次进度事件、工厂用户视图简化、四轮页面验收结论与全部缺陷修复记录；标注一期实现状态 |

---

## 1. 交付目标与验收边界

一期交付目标：构建**可运行的短期排产研究演示平台**——以一份生产计划为主对象，从标准 XML 接口实例接入需求与供应，经版本化基础资料引用、输入快照固化、多策略排程求解、离散事件仿真与独立校验，形成可比较、可审批、可追溯的方案；另建生产量级蒙特卡洛链路支撑统计分析。

验收边界（不冒充实现）：ERP/MES 实连与生产发布、冻结/已投产保护、长期计划引擎（N+3/N+6/N+1）、完整钢铝比例与小颜色批量规则、多产线并行调度、动态插单/撤单实际生效、人工拖动排程均**不在一期范围**；页面不提供假按钮、无效字段或"即将支持"的空表单。

演示数据与历史生产事实严格区分：结果必须由真实计算生成，不预置、不缓存回放、不删库清历史。

---

## 2. 总体架构与技术栈

### 2.1 架构总览

```
企业场景输入（XML 接口实例，test-suites-xml）
        │
        ▼
xml_adapter.py     XSD结构解析 → Scenario 领域模型（订单/配置/资源/缓冲/物料/事件）
        │
        ├──► business.py        版本化基础资料：产线/节拍/日历/缓冲/切换的提取、校验、XML回写与准入
        │
        ├──► aps-fullchain      小场景全链路内核（12–17辆）：
        │     净额冲销 → 聚合配额 → 逐车分配 → 四策略派工 → 离散事件仿真 → 独立校验 → 字典序评价
        │
        └──► production_sim.py  生产量级蒙特卡洛内核（4200辆·5日双班·秒级）：
              1000次到货/加工/故障联合扰动抽样，四方案共享同一样本（CRN），统计聚合
        │
        ▼
server_integrated.py   REST API + SQLite 持久化 + 后台任务线程 + 逐次进度事件 + 角色工厂隔离 + 状态门禁
        │
        ▼
web/（原生JS工作台）
        计划管理 → 场景选择 → 计划依据 → 生成方案（业务进度） → 调度结果 → 参考排程 → 提交/审批
```

### 2.2 企业集成架构与数据流向（生产化设计）

```
[外部业务系统]
├── SAP-ERP  (月度/周度销售预测与工单) ───┐
├── SRM/SCM  (供应商送货与延期通知)  ───┼──► [API Gateway / ESB] ──► [Kafka/RabbitMQ]
├── MES      (生产工位打点与设备停线) ───┤        │ TLS加密/鉴权            │ 可靠消息持久化
└── WMS      (线边立库物理库存与领料) ───┘        │                        ▼
                                                │             [APS 接口适配服务]
                                                │             ├── 1. XSD 模式强校验
                                                ▼             ├── 2. 消息幂等排重
                                       [HTTP/REST 报警]       ├── 3. XML → 领域模型转换
                                                              └── 4. 驱动全链路调度/重排
```

一期演示以 `xml_adapter.py` 承担适配服务职责（XSD 结构校验、净额冲销、领域模型转换）；网关、消息队列与双向往返为生产化设计，未实现。

### 2.3 技术栈与选型说明

| 层 | 当前实现（一期） | 生产化选型方向（设计预留） |
|---|---|---|
| 前端 | 原生 JS + CSS（零依赖），语义表单/details/table，静态服务同源 | Vue3/React18 + TypeScript、Canvas/SVG 虚拟滚动甘特、SSE/STOMP 推流 |
| 通信 | fetch + 轮询（进度事件 350ms 增量拉取、断线退避） | SSE/WebSocket 长连接，断线按 plan_id 重连 |
| 后端 | Python3 标准库 ThreadingHTTPServer，后台线程任务 | 生产级 WSGI/异步框架 + 任务队列 |
| 存储 | SQLite（plans/tasks/catalogs/关系表/JSONL样本） | PostgreSQL 14+（DDL 已按其规范设计，见 §6） |
| 算法 | 确定性启发式 + ALNS + 离散事件仿真（标准库实现） | 可插拔 MILP 基准（有求解器时增加，不虚构已安装） |

设计原则：以生产计划为核心业务实体（一级计划列表 + 二级工作台，不以菜单为流程载体）；服务端强校验与工厂行级隔离；接口契约驱动（B2MML/OAGIS 风格 XML 接口实例）；决策与执行状态解耦——"无接受不执行"。

### 2.4 信息隔离与防泄漏

计划与执行分离：决策屏障前（Phase 1）释放的历史信息固化，后续派工不得使用；外部事件以 occurred_at / known_at 双时间记录，当前求解仅使用起点已知信息，未来事件仅展示不生效。输入快照不可变，任务保存完整快照与输入 hash，审计覆盖导入/规则修改/调整/批准/提交。

---

## 3. 标准数据契约

采用独立 schema `aps_fullchain_v1`（XML 接口实例）/ `aps_fullchain_v2_business`（含基础资料版本）/ `aps_production_weekly`（生产量级）。

| 对象 | 必备字段与规则 |
|---|---|
| calendar | timezone、日期到分钟映射、按资源的工作窗口、明确观测截止 |
| forecast | forecast_id、来源版本、月份、工厂、配置、数量、发布时间 |
| order | order_id、version、车辆id、配置、route、release_at、due_at、priority、forecast_bucket、状态 |
| configuration | 车型/颜色/钢铝/特殊配置、资源资格、逐工序时长、逐工序 BOM |
| resource | resource_id、stage、日历、期初属性、capacity；一期每工序单启用资源（Capacity 强制 1），多资源字段保留 |
| labor | skill_pool、逐日历可用人数、准备/加工/阻塞值守需求 |
| supply | material_id、期初净库存、承诺到货、数量、可用时点、来源版本 |
| buffer | edge、capacity、运输时长、取车纪律（FIFO） |
| baseline | 已批准版本、车辆日分配、各工序固定承诺、冻结边界 |
| execution | 当前分钟、在制位置、剩余加工、阻塞/缓冲车辆、实际库存、设备末属性 |
| event | event_id、occurred_at、known_at、type、结构化 payload（禁止对象 repr）、revision |
| plan | id、parent_version、input_hash、rule_version、类型、期间、量/序列/策略、状态 |
| task | id、输入hash、算法、种子、预算、实际阶段、开始结束时间、异常 |

数量守恒：原始预测 = 已冲销预测 + 剩余预测；净需求 = 有效实单 + 剩余预测。虚拟预测车独立 ID，不与实单混淆。时间保留业务时区与事件发生时间；车辆计数整数并守恒。

接口逻辑清单（IF-01～IF-07）：销售/订单→APS、ERP主数据→APS、供应→APS、APS→ERP（预测信号）、APS→MES（排序，发布单元+序位+车辆映射+业务回执）、MES→APS（投产/完工/异常，幂等+补数）、接收方→APS（对账）。每项接口联调前签署字段、责任人、刷新频率、延迟、错误码与恢复方式；无 API 时受控文件/人工回执，不降低核对标准。

### 3.1 报文接入处理流程（一期以 xml_adapter 实现）

```python
def parse_and_ingest_xml(xml_content: str) -> IngestionResult:
    # 1. XSD 强模式校验
    schema = load_xsd("ApsInterfaceSchema.xsd")
    if not schema.validate(xml_content):
        return IngestionResult(success=False, error_code="XSD_VALIDATION_ERROR", details=schema.errors)
    # 2. 消息幂等检查（MessageHeader.MessageId + ContentHash）
    header = extract_header(xml_content)
    if is_message_processed(header.message_id):
        return IngestionResult(success=True, status="IDEMPOTENT_IGNORED", message_id=header.message_id)
    # 3. 反序列化为内部 Scenario 领域模型
    scenario = xml_to_scenario_domain(xml_content)
    # 4. 数据准入校验（无负库存、无零缓冲、无未知配置）
    admit = validate_scenario_admission(scenario)
    if not admit.passed:
        return IngestionResult(success=False, error_code="ADMISSION_REJECTED", details=admit.reasons)
    # 5. 固化快照并入库
    save_snapshot_and_orders(scenario)
    return IngestionResult(success=True, scenario_id=scenario.scenario_id, content_hash=header.content_hash)
```

一期实现状态：步骤 3（解析与净额）、4（准入与缺项阻断）、5（快照固化）已实现并经页面验收；XSD 强校验与消息幂等排重为设计保留（演示环境单机单批次场景未启用消息层）。

### 3.2 核心接口实例处理语义（按场景）

| 场景实例 | 来源系统 / 消息类型 | 处理时序与动作语义 | 一期状态 |
|---|---|---|---|
| case_01 正常基准（16 辆工单） | SAP-ERP / PRODUCTION_DISPATCH_SCENARIO | 接入报文提取工单与 BOM → 物料与产能准入 → 需求冲销净额核算 → 多算法并发仿真推荐最优 → 复核提交 → 审批下发并取回执 | 链路已实现；"下发取回执"为演示状态记录，非 MES 实连 |
| case_02 物料到货延迟 / 压力断料 | SRM·SCM / SUPPLY_DELIVERY_INBOUND（MATERIAL_DELAY 事件） | 事件获知时刻触发决策屏障暂停派工 → 在制快照锁定 → 未上线车辆按新到货时间重排 → 断料饥饿时压制下游投产生成子版本 v2，回执确认后继续执行 | 输入与仿真生效已实现（延迟影响完工结果可验证）；"决策屏障+子版本续算"生产化机制未实现 |
| case_04 紧急 VIP 插单 | 销售中台 / RUSH_ORDER_NOTIFICATION | 冲销判定（同桶未投产虚拟预测车等量替换，总需求守恒；超额则增至 17 辆）→ 高优先级排队置换 → 变更版本与差异台账 | 冲销与排队优先已实现；"插单实际生效的动态重排"未实现（事件仅展示，计算以起点信息为准） |
| case_05 订单撤销与在制保护 | SAP-ERP / ORDER_CANCEL_NOTIFICATION | 未启动 Node B 车辆撤除排产授权（assigned_day=None）移出队列；已上线车辆打 CANCELLED_IN_WIP 保护流转至出线，绝不强行剔除在制；后续车辆前移填补 | 取消未开工剔除已实现；在制保护分支仲裁与滚动重排未实现（边界已在页面声明） |

---

## 4. 算法设计（aps-fullchain 全链路）

### 4.1 需求净额与生命周期（FR-02）

同源同版本幂等，旧版不覆盖新版；仅按已确认工厂/月份/配置桶冲销。已完工退出待排进入实绩；未完成保留原 ID 结转；已投产取消转异常，不物理删除。跨来源同单依赖业务映射，不按配置+数量去重。

### 4.2 聚合供需计划（FR-04/05，一期未接引擎）

整数增量分配设计：先安排必须保留量，再按优先级逐批增加，每次增量检查跨月累计物料与能力；N+3/N+6 重合月一致映射。聚合层不承诺序列可行，资源预留与缺口向下传递。**一期状态：设计保留，引擎未开放，不拿 W+3 引擎冒充。**

### 4.3 W+3 周计划与逐车排序（FR-06/07）

月度日能力内订单分配到日，保留来源与车辆实例标识；预检受限配置日上限、批量与间隔。排序处理批次、比例、颜色、间隔、车型/地板/顶棚切换及特殊车规则；校验班次与跨日连接处。总装与焊装顺序不同；其他车间车序标注"预测"。

### 4.4 候选策略与预算（FR-08）

四策略共用同一事件解码与校验器：A 交期优先（EDD/重要度）；B 颜色感知+下游工时平衡；D 负荷平准；E ALNS（破坏算子：最迟交期/高切换/随机；修复算子：最少切换贪心/Regret-2；SA 准则；8 级字典序方向）。预算按候选评价次数与墙钟双记录；单次仿真可取消/超时。产品不限定单一算法，不要求证明全局最优；到时有解仅成候选不自动批准。

### 4.5 生产仿真引擎

- **四节点状态机**：每工序 B（准备开始）/S（加工开始）/C（加工完成）/F（设备释放）；设备 OFF/IDLE/SETUP/PROCESSING/BLOCKED/DOWN，车辆 UNRELEASED/READY/…/COMPLETED/CANCELLED。准备+加工用真实日历/人员/物料，默认不可跨工作窗口；加工完成后 BLOCKED 可跨休息，占设备不占加工人员。
- **有限缓冲与正阻塞**：下游无空位则上游车辆 BLOCKED（C 已定 F 未定），设备不接新车；下游取车后同刻迭代唤醒，直至无新增可推进事件。运输占容量不可被提前选取。拔出车 P 后立即退出，不进 P-A 缓冲、不耗电池。
- **车间独立派工**：设备空闲时从已到达、资格合法、物料可保障、人员可用且未违反冻结的候选中选取；W 来自日/周计划，P/A 来自各自缓冲，各阶段独立优先规则。
- **库存与人员**：B 前检查/预留，S 实际扣减并释放预留；加工人员 [S,C) 占用，准备人员 [B,S) 占用；同刻联合处理释放与申请；物料逐事件非负，人员逐时不超上限。
- **事件优先级**：同刻批处理——①完成释放人员；②到货/日历/人员恢复/已知更新；③下游取车与原子预留；④缓冲释放后上游出站传播；⑤新准备/加工开始与领料；⑥零时间传播至不动点。同刻竞争按资源 ID+车辆 ID 定序，不依赖字典序或线程调度；零时间循环设事件数上限。
- **停滞兜底**：未完成车辆但无未来事件时返回"截至观测期未完成/资源阻塞/待料"及车辆列表，不算完工、不无限延长工作日。
- **独立校验**：13 项物理不变量（四节点时序、工位互斥、工序先后与转运、缓冲容量、物料节点扣减与非负、技能池并发、拔出路径等）由独立校验器执行，与求解解耦。
- **字典序评价**：可行性→未排数→未排权重罚→加权拖期→稳定性→换色→阻塞→makespan；"完成准时率"按末道工序 F 与交期对比（未完工为 null，不冒充 0）。

---

## 5. 生产量级蒙特卡洛内核（production_sim）

### 5.1 模型定位

秒级等效瓶颈流水 DES：3 车间 W→P→A 串行、车间间并行、有限缓冲（各 40）、正阻塞不丢车；单车间为单产能槽（瓶颈口径），**非多工位数字孪生**。自然日 86400s 与双班工作窗（06:00–13:40、14:20–22:00，各 460min）分离量纲；加工非工作窗暂停顺延，到货/故障/释放/交期同一墙钟。

### 5.2 场景输入（4200 辆演示）

6 配置（SUV/SEDAN/MPV/PICKUP × 颜色，含拔出配置）、基础节拍 W62/P58/A60 秒、切换 45–95s（W/A 按车型、P 按颜色变更计）；5 物料（钢/铝/漆/电池/电机）分 6 批到货；期初库存 15–20% 需求量；订单释放分散于 10 个工作窗，交期=释放+1.8 班次（截断至观测终点）。

### 5.3 扰动抽样与 CRN 公平

每样本一次性生成、四方案共用：①到货 σ=720s 高斯；②逐车逐工序时长精确对数正态（真实 CV=0.08，σ=√ln(1+cv²)，μ=−σ²/2）；③各线故障 Poisson(1.1)、条件均匀到达、指数修复均值 40min、末端截断，可为零。样本 seed=base+idx 完全可复现；策略间不重采样。全部参数为研究假设，未经现场标定。

### 5.4 策略与优化语义

EDD / COLOR_GROUP（涂装优先同色）/ LOAD_BALANCE（总装优先同车型，车型连续性口径）/ OPTIMIZED（名义无扰动场景上候选选优：优先级/EDD/颜色分组/车型分组/确定性 2-opt，按完成率→整体按期→条件按期→阻塞→makespan 打分取最优固定序列；全阶段按该序列执行，不随样本重优化、不读未来）。

### 5.5 统计口径与准出

三分母指标：完成率、整体按期率（÷有效需求）、条件准时率（÷已完成，无完成则未定义）；拖期分"全部已完成/仅拖期车"两分母；聚合 mean/std/P05/P95/95%CI（正态近似）与有效样本数。仅全部 1000 样本成功才 `complete`；任何错误保留并 `not_complete`；取消保留已完成计数不输出均值。物理不变量独立校验（唯一开工/工序先后/库存非负/台账守恒/缓冲上限/W→P 与 P→A 流守恒/完成口径会计），违规样本 failed 且不计入统计。逐次进度事件（seq/done/phase）持久化供页面真实播放。

---

## 6. 数据库物理设计

一期 SQLite 与生产化 PostgreSQL DDL 同构。核心表：

- `aps_plan_header`：plan_id、plan_name、plan_type、plant_code、status（DRAFT/RUNNING/READY/PENDING/APPROVED/SUPERSEDED/FAILED）、version、parent_plan_id、input_hash、selected_candidate_id、审批人与时间、审计字段；索引 (plant_code,status)。
- `aps_plan_detail`：逐车逐工序 plan_id/vin/order_id/config_id/assigned_day/sequence_position/stage/resource_id/node_b_s_c_f/setup/process/blocked/is_pullout/execution_status。
- `aps_plan_candidate`：strategy_name、is_valid、unmet_count、加权拖期、阻塞、换色、makespan、稳定性罚、综合分、is_selected。
- `aps_order_master`：vin(PK)、order_id、plant_code、config_id、model_type、color、material_type(STEEL/ALUMINUM)、is_pullout、priority(1=VIP/2=普通/99=预测残差)、priority_weight(10/5/1)、release_at_min、due_at_min、forecast_bucket、is_synthetic（虚拟预测车标记）、lifecycle_status(UNRELEASED/DISPATCHED/WIP/COMPLETED/CANCELLED)、source_message_id；索引 (plant_code,lifecycle_status)、(forecast_bucket)。
- `aps_material_ledger`：ledger_id、material_id、plant_code、batch_no、on_hand_qty、reserved_qty（Node B 预锁）、available_at_min、known_at_min（防未来信息穿越）、source_version、is_received；索引 (material_id,plant_code,available_at_min,is_received)。
- `aps_dispatch_receipt`：receipt_id(PK)、baseline_id、plan_id、plan_version、target_shop(WELD/PAINT/ASSEMBLY/ALL)、dispatched_by、dispatched_at_min、receipt_status(CONFIRMED/REJECTED/TIMEOUT)、response_code、signature_hash（报文签名）；索引 (plan_id,plan_version,receipt_status)。**一期为设计预留，未实连下发。**
- `aps_event_replan_log`：log_id、epoch_min（决策屏障时刻）、trigger_event_ids/types、prefix_records_count + prefix_hash（冻结前缀全量哈希）、parent_plan_id→child_plan_id、receipt_id、wip_protected_vins、replan_unmet_vins、unstarted_adjustments（日期位移明细）。**一期为设计预留，滚动重排未实现。**
- `catalogs`：版本化基础资料（id=工厂:场景、revision 乐观锁、checks 缺项清单、updated_by/at）。
- `tasks`：任务、progress（含 cancel_requested）、输入快照引用；样本明细落 `PROD_DATA_DIR/<task_id>/`（samples.jsonl、progress.jsonl、result.json、sample0_timeline.json），库内仅存摘要引用。

状态机：草稿→计算中→计算完成/失败→待审批→已批准；提交绑定候选且未完成/校验不过返回 422；批准后锁定（禁换场景/换版本/重算/换候选）；生产统计不经提交链路。计算任务状态独立（排队/运行/完成/到时有解/未找到/失败/取消）。

---

## 7. 用户端实现

### 7.1 页面结构（工厂用户视图，prod-operations-2）

- **计划管理**：列表（搜索/状态筛选/空态）、新建弹窗（名称+工厂）；无预置计划与结果。
- **场景选择**：弹窗"当前为演示数据，请选择你要测试的场景"，业务名称按钮；同一计划内一次载入，无独立菜单、无二次建单。
- **计划依据**：规模/日历/缓冲指标卡、产能节拍表、订单明细（搜索+分页 42 页至 V04199）；可折叠。
- **生成调度方案**：按钮"开始计算"；业务语言状态（排队中/准备基准排程/多情景评估中/完成/取消/失败）+ 不确定进度条；无实验计数、失败样本、种子等技术字段（仅后台日志）。
- **调度结果**：四方案对比表——预计完成率、整体按期交付率、已完成订单准时率（口径注明）、平均拖期（自动分钟/小时）、预计完成/未完成；标注"预测/仿真估计"；统计均值不可提交。
- **参考排程**：单次模拟时间表，按方案/产线/车号筛选分页；时间"第N天 HH:MM"；明确非平均方案、非生产指令。
- **提交与审批**：Fail-Closed 门槛（未完成/校验不过禁提交并显示原因）；页内模态确认绑定内容；主管待办批准；批准后锁定。
- **基础数据**（工艺负责人）：产线（启停/备用）、节拍（空值可存但阻断）、日历（班次窗编辑）、缓冲/转运、切换规则；版本号+乐观锁（409）+缺项清单。

### 7.2 前端机制

原生 JS 组件（workbench.js + production-web.js）；点击委托 data-act/data-pa；弹窗焦点圈与 Escape；进度按事件 seq 逐条应用、只 patch 不重建（不打断用户筛选）；断线退避显示"等待恢复"；离开计划/切身份停止轮询；刷新按快照恢复不重放；资源带版本参数防缓存。

### 7.3 端到端时序

**一期演示口径（已实现）**：场景载入（净额冲销守恒核验）→ 发起计算（后台多候选并行仿真+独立校验+字典序推荐）→ 页面进度事件流 → 结果看板与参考排程 → 提交绑定候选 → 主管批准绑定内容版本。

**生产化时序（设计保留，未实现）**：完整 mermaid 时序图见历史文档《APS系统用户端与系统对接技术实现方案》§5，含两条链路——①正常编制发布：计划员试算 → 独立校验 13 项不变量 → 8 级字典序推荐 → 提交锁定内容版本 → 主管审批 → 签名下发 MES → 回执 RECEIPT_CONFIRMED → 状态"已生效"；②动态扰动滚动重排：外部事件到达 → 决策屏障暂停 Phase 2 派工 → 提取 [0,τ] 冻结前缀（前缀哈希锁定）→ 在制 WIP 打标保护、未开工缺料车撤授权 → 净额与周日重排生成子版本 → 回执确认后注入授权集合续算至视界末期，全程记录重排台账。

---

## 8. 验收与质量记录

### 8.1 验收原则（硬约束）

仅真实浏览器操作（点击/输入/读取可见响应/截图）作为验收证据；禁止直接读库/业务文件、HTTP 断言、浏览器 fetch、内部函数或 localStorage 伪造状态。空库起步、测试计划经页面创建并保留；100% 用例通过 + 0 未修复阻断缺陷才准出。

### 8.2 四轮验收结论（2026-09-19）

| 轮次 | 环境 | 范围 | 结论 |
|---|---|---|---|
| CLEAN 全量 | 18745 | T01–T36 小场景全功能 | 34 通过 + T26/T27 边界展示通过；早期违规 fetch 轮次已否决重测 |
| 生产量级 | 18746 | P01–P15 + 6 项缺项 | 全部通过；1000/1000 失败 0；运行中筛选不打断；取消保留 215/1000 |
| 进度可视化 | 18746 | 逐次真实进度 | 通过；132→139→145→152→159 递增；刷新恢复；筛选保持时 279→300 |
| 工厂用户视图 | 18746 | 业务界面简化 | 通过；13 项技术字段零出现；订单分页至 V04199 |

辅助确认：fullchain 核心 91 项测试、API 4 项（含长期引擎 422 拒绝、跨厂隔离）；二者覆盖用例内正确，不替代页面验收，不宣称全工厂规则正确性。

### 8.3 关键缺陷与根因修复记录

| 阶段 | 缺陷 | 根因与修复 |
|---|---|---|
| V1 修复 | 前端样式整文件覆盖+42 处弯引号 | 无备份事故；按未损坏截图重建 style.css，引号全修复 |
| V1 修复 | 场景卡片点击 404；场景库异步竞态空白 | 场景 ID 误当计划 ID；异步加载后补渲染 |
| V1 修复 | 提交门槛缺失；按期率/拖期天数失真 | 服务端 submittable/submit_blockers 强制校验（422）；on_time_rate 按 F 时刻真实派生；lateness_days 未完工为 null |
| V1 修复 | 【高影响】净额恒多 N 台未排 | 合成预测桶键（FACTORY_1）与订单前缀（P1）不匹配致需求翻倍；改按订单自带 ForecastBucket 聚合 |
| V1 修复 | RUSH_ORDER 崩溃 | 夹具把对象 repr 塞 payload；改结构化属性并拒绝 repr |
| V2 修复 | 旧约束页与新 XML 链路脱节（字段空白/修改无效） | 统一"场景XML→版本化基础资料→快照→同一解析→计算"单链路，废弃双编辑器 |
| V2 修复 | 未完成口径漏洞（已排未完成漏判提交） | 授权集合取真实 daily_allocations+实际节点；三类未完成（未排入/已排未开工/已开工未完工）任一阻断提交；day_length 归一 |
| 生产量级修复 | 内核队列丢车/堆错出队/换型状态缺失 | 选择只读+物料通过才出队；按已开工集合惰性删堆；W/P/A last_attr 开工时更新 |
| 生产量级修复 | 故障抽样失真、CV 口径错误 | 改标准泊松过程（条件均匀+指数修复）；CV 为真实变异系数 |
| 集成修复 | 详情页空白（数字单元格 appendChild 崩溃） | 单元格类型分派渲染；异常兜底错误卡 |
| 集成修复 | 进度假死 0/1000、取消无反馈 | 任务本体映射错误致轮询中断；activeTaskId 全状态设置；350ms 事件流替代粗轮询 |
| 集成修复 | planRef 未定义渲染失败；资源缓存旧版本 | 引用修复；index 资源带版本参数 |
| 产品修复 | 生产量级误做独立菜单/二次建单 | 回归"计划管理内场景"，同 plan.id/name 绑定 |
| 产品修复 | 订单分页按钮被拦截；缓冲字段 undefined | 展开时序与渲染层级修正；stage 映射兜底 |

### 8.4 Mock 实测参考（本机单次运行，非服务等级承诺）

小场景基准（16 辆、三车间、有限缓冲 B=2）：ALNS 将涂装换色 9→5 次（−44.4%）、正阻塞 62→42 分钟（−32.3%），25 次受控迭代 0.051 秒；36 场复合扰动矩阵（16/32/48 车 × 正常/双重延期减员/插单撤单/颜色铝瓶颈）全闭环通过独立校验。生产量级批次（4200×1000×4）完整完成、失败 0；四方案完成率均值 0.5751/0.5757/0.5752/0.6258。以上为演示环境数据，不是工厂绩效，详见论文第四章。

---

### 8.5 生产化实施路线（设计保留，非一期承诺）

| 阶段 | 周期 | 内容与准出 |
|---|---|---|
| 一、标准接口接入与用例库集成 | 第 1–2 月 | 企业集成网关部署；接入 25 个标准 XML 用例；XSD 校验管道与消息幂等存储；负库存/超缓冲非法报文 100% 自动拦截 |
| 二、用户端与看板上线 | 第 3–4 月 | Vue3/React 计划大厅与 6 步工作空间；对接排程仿真内核；字典序选优看板与正阻塞 Canvas 甘特 |
| 三、车间双向试点联调 | 第 5–6 月 | 一工厂总装/涂装试点，双向联调 MES 报文与 WMS 库存；验证断料/插单在线滚动重排，达成生产级交付验收 |

该路线为生产化演进建议，工期未做人力评估；一期演示不等同于其中任一阶段完成。

---

## 9. 工程约束与协作门禁

- 模型使用：禁止安得/* 模型处理本项目内容；子 Agent 须在模型可核实且获用户授权后方可接收项目资料；不修改用户模型/供应商配置，不声称客户端已全局禁用。
- 服务隔离：18745 为 V2 小场景验收环境、18746 为生产量级演示环境，不混用；演示库只增不清，删库不作重置手段。
- 文档一致性：需求对应见 PRD V2.0 附录 A（FR 实现状态）；本文与 PRD、验收记录三者不一致时，以最新修订记录并同步修订为准。
- 变更纪律：样式/引号事故后建立"改前读原文、改后语法检查（node --check / py_compile）、页面复测"流程；浏览器自动化仅用真实页面操作链路。

---

## 附：技术资料对照

| 本文档章节 | 对应资料 |
|---|---|
| §1–§5 | 全链路仿真实施设计、生产量级场景与仿真说明 |
| §2.2/§6/§7 | 用户端与系统对接技术方案（PostgreSQL DDL 全文与生产化时序图） |
| §7.1/§8 | 业务可用性重构方案、页面级验收清单、页面验收要求 |
| §8.2–8.4 | 验收结果与算法说明、四轮页面验收留档 |
